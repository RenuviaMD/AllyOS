name: AllyOS daily audit & health

# The two Master-Console agents, run daily + on demand ("Run now" = workflow_dispatch).
on:
  schedule:
    - cron: '0 4 * * *'      # 04:00 UTC daily
  workflow_dispatch: {}       # manual trigger from the Actions tab (the admin "Run now" button)
  push:                       # CI gate — also run on every push that touches AllyOS
    paths: ['allyos/**', 'protocols/**', 'netlify/**', 'scripts/**', '.github/workflows/daily-audit.yml']
  pull_request:
    paths: ['allyos/**', 'protocols/**', 'netlify/**', 'scripts/**']

permissions:
  contents: read

jobs:
  health:
    name: Debug / Health agent
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - name: Audit codebase (JSON · inline JS · functions · calc · links · refs)
        run: python3 scripts/health-check.py
      - name: Upload status (read by the Master Console)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allyos-status
          path: allyos/admin-status.json

  # Operations Auditor — re-verifies clinical content (citations resolve, label
  # doses/contraindications current, no drift) via the Claude API on the highest
  # model. Requires the ANTHROPIC_API_KEY repo secret; wired from the cockpit.
  #
  # ops-audit:
  #   name: Operations Auditor (Opus)
  #   runs-on: ubuntu-latest
  #   needs: health
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Re-audit clinical content
  #       env:
  #         ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
  #       run: python3 scripts/ops-audit.py    # multi-Opus-auditor pass; emits a needs-fix report
